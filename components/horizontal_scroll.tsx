type HorizontalScrollProp = {
  /** JSX elements to be scrolled horizontally */
  tiles: JSX.Element[];
};

/** Horizontal scroll bar for tiles, as seen in interfaces like the Netflix mobile app
 *
 * @example
 *
 * <HorizontalScroll tiles={weather_tiles} />
 */
const HorizontalScroll = (props: HorizontalScrollProp) => {
  return <></>;
};

export default HorizontalScroll;
